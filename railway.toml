[build]
builder = "nixpacks"

[deploy]
startCommand = "dotnet BarberPro.dll"
healthcheckPath = "/swagger/index.html"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
